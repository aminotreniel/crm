services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: financial_crm
      POSTGRES_USER: financial_crm
      POSTGRES_PASSWORD: local_development_only
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
